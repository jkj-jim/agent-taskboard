# 更新日志

每个已发布版本一个小节，标题就是 `package.json#version`。发布 `app-vX.Y.Z` 时
release workflow 会抽取对应小节作为 GitHub Release 的说明正文；正式版缺这一节
会直接让发布失败，预发布版缺了就只带《首次打开》说明。

面向使用者写「这一版能做什么、有什么变化」，不是提交记录的罗列。

## 0.1.1

修复自动更新。

0.1.0 的更新元数据指向的下载地址是错的：产物本地叫 `Agent Taskboard.app.tar.gz`，GitHub 上传资产时会把空格换成点，而元数据里拼的是本地文件名，于是应用去取更新时拿到 404。Release 页面和下载链接都是好的，只有自动更新受影响。

装了 0.1.0 的话，本版本发布后应用内更新即可恢复正常，不需要手动重装。

## 0.1.0

首个 macOS 桌面版本。

**桌面应用**

- macOS 14+ Apple Silicon 安装包，自带 Node 运行时，本机装没装 Node 都能用。
- 数据落在 App Data 而非应用包内，卸载（删除 `.app`）不会触碰任务、评论和附件。
- 正式版与预发布版各用一份独立数据目录和端口，预发布版不读写正式版数据，也不改动共享的 Skill。
- 关闭窗口只隐藏，应用留在程序坞；Cmd+Q 才退出。
- 通过 GitHub Releases 自动更新，更新产物带签名校验。

**Agent**

- 顶部状态区展示 Codex、Claude Code、WorkBuddy 三个 Agent 的实时可用状态。新任务只能派给可用的 Agent；已有任务的负责人不受状态变化影响。
- WorkBuddy 的 MCP 接入改成一键配置：看板写好配置并验证 MCP 端点可用，再把你带到 WorkBuddy 的信任对话框前，授权仍由你点。
- Codex 任务启动不再降级为无头执行——自动化不可用时如实报失败，会话该出现在 Codex 侧栏就一定出现在那里。
