# Agent Taskboard 开发入口

## 任务驱动

- 用户在 Taskboard `agent-taskboard` 项目下建的任务是任务主入口：拿到任务号先用 `manage-taskboard` skill 读取任务和全部评论。
- 对话中直接提出的需求不强制归入 Taskboard，按对话推进即可。识别出值得长期跟踪的工作（成体系的功能、需要交接或验收的改动、跨多轮的返工）时，说明理由并询问是否归入；用户同意后再用 skill 查找同类任务，没有再创建。微小改动直接做，不必提议。
- 当前用户指令、任务描述和全部评论共同构成任务上下文。新要求、返工意见与验收结果写回同一任务，不另建重复的任务文档。
- 项目、任务、关系和评论操作全部走 `manage-taskboard` skill；状态、version、并发认领、branch/worktree 和评论规则按需从该 skill 加载，本文件不重复维护。生命周期保持 `todo → in_progress → in_review → done`，只有用户明确验收才进入 `done`。
- 本文件是 Codex 与 Claude Code 的共享入口（`CLAUDE.md` 软链到本文件）。涉及 Agent 能力、负责人、会话、唤起、Skill、模型、权限或配额的改动，先核对 `shared/agents.mjs` 和 `server/agents/` 两端适配器；共享逻辑不按 Agent 名称硬编码分支，单端能力要明确边界并确认另一端主路径不受影响。
- `README.md` 维护稳定的使用说明，本文件只维护项目级协作入口，代码与配置反映当前实现。单次方案、过程和进度留在任务中。

## 执行闭环

1. 开始前用 skill 的 `issue brief` 一次读取任务、全部评论和附件，确认最新范围、验收口径、状态和开发上下文；本仓库任务必须归属现有 `agent-taskboard` 项目，不写入 `Local`。
2. 用 `rg` 定位真实入口，再只读直接相关的组件、API、配置或文档；先证明「入口 → 操作 → 数据变化或副作用 → 可观察结果」，需要时再渐进扩展上下文。
3. 实现最小可用闭环，不遗漏、臆断或顺手扩展需求。优先复用和低耦合；方案不明显时可以多投入研究，但只采用能降低复杂度、返工或长期成本的设计。
4. 按风险验证直接路径，UI 与交互在真实产品中检查准确性、可用性和观感。用户确认前不新增推测性防护、兼容层或回归测试。
5. 完成后按 skill 写一条精炼的交付评论并移至 `in_review`；返工继续更新同一任务，明确验收后再移至 `done`。

## 月度更新

- 已完成的新产品逻辑或项目运行机制记入 `document/updates/<YYYY-MM>.md`，只写最终结果。它不是任务依据，也不进入默认读取链。
- 需要写入或查询更新记录时，先读 `document/updates/RULES.md` 取写法与有界读取命令，不要凭记忆操作，任何情况下都不要通读月文件。
